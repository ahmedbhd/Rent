<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="titetextstyle" parent="TextAppearance.AppCompat.Medium"></style>

    <style name="subtitetextstyle" parent="TextAppearance.AppCompat.Caption"></style>

    <style parent="Theme.AppCompat.Light" name="FlatButton">

        <item name="android:textColor">@color/myPeach</item>

        <item name="colorControlHighlight">@color/myRed</item>

    </style>

    <style parent="Theme.AppCompat.Light" name="FlatButton2">

        <item name="android:textColor">@color/teal_700</item>

        <item name="colorControlHighlight">@color/blue_800</item>

    </style>
</resources>
